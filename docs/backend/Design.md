# 🎨 Horizon Credit Repair — Backend Design Documentation

---

## 1. System Architecture Overview

### 1.1 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           HORIZON BACKEND ARCHITECTURE                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   CLIENTS                                                                   │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│   │   Web    │  │   iOS    │  │ Android  │  │ Desktop  │                   │
│   │  Client  │  │   App    │  │   App    │  │   App    │                   │
│   └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘                   │
│        │             │             │             │                          │
│        └─────────────┴──────┬──────┴─────────────┘                          │
│                             ▼                                               │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                        API GATEWAY (Kong)                           │  │
│   │   • Rate Limiting  • Auth  • Routing  • SSL Termination             │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                             │                                               │
│        ┌────────────────────┼────────────────────┐                         │
│        ▼                    ▼                    ▼                         │
│   ┌──────────┐        ┌──────────┐        ┌──────────┐                    │
│   │   Core   │        │    AI    │        │  Worker  │                    │
│   │   API    │        │ Services │        │ Services │                    │
│   │ (NestJS) │        │(FastAPI) │        │ (BullMQ) │                    │
│   └────┬─────┘        └────┬─────┘        └────┬─────┘                    │
│        │                   │                   │                           │
│        └───────────────────┴───────────────────┘                           │
│                             │                                               │
│   ┌─────────────────────────┴─────────────────────────────────────────────┐│
│   │                          DATA LAYER                                   ││
│   │  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐          ││
│   │  │Postgres│  │ Redis  │  │MongoDB │  │Elastic │  │Pinecone│          ││
│   │  │  (SQL) │  │(Cache) │  │ (Docs) │  │(Search)│  │(Vector)│          ││
│   │  └────────┘  └────────┘  └────────┘  └────────┘  └────────┘          ││
│   └───────────────────────────────────────────────────────────────────────┘│
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 Microservices Breakdown

```
SERVICE DECOMPOSITION

┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│  CORE SERVICES                                                      │
│  ─────────────                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │     Auth     │  │   Customer   │  │   Billing    │              │
│  │   Service    │  │   Service    │  │   Service    │              │
│  │              │  │              │  │              │              │
│  │ • Login      │  │ • Profiles   │  │ • Stripe     │              │
│  │ • Register   │  │ • Settings   │  │ • Invoices   │              │
│  │ • OAuth      │  │ • Documents  │  │ • Refunds    │              │
│  │ • MFA        │  │ • Export     │  │ • Dunning    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                     │
│  CREDIT SERVICES                                                    │
│  ───────────────                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │    Report    │  │   Dispute    │  │  Monitoring  │              │
│  │   Service    │  │   Service    │  │   Service    │              │
│  │              │  │              │  │              │              │
│  │ • Fetch      │  │ • Create     │  │ • Alerts     │              │
│  │ • Parse      │  │ • Track      │  │ • Changes    │              │
│  │ • Store      │  │ • Submit     │  │ • Scores     │              │
│  │ • Analyze    │  │ • Letters    │  │ • Reports    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                     │
│  AI SERVICES                                                        │
│  ───────────                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │   Analyzer   │  │   Chatbot    │  │     OCR      │              │
│  │   Service    │  │   Service    │  │   Service    │              │
│  │              │  │              │  │              │              │
│  │ • Scoring    │  │ • NLP        │  │ • Extract    │              │
│  │ • Detection  │  │ • Context    │  │ • Parse      │              │
│  │ • Prediction │  │ • Handoff    │  │ • Classify   │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                     │
│  SUPPORT SERVICES                                                   │
│  ────────────────                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │Notification  │  │   Workflow   │  │    Admin     │              │
│  │   Service    │  │   Service    │  │   Service    │              │
│  │              │  │              │  │              │              │
│  │ • Email      │  │ • Rules      │  │ • Dashboard  │              │
│  │ • SMS        │  │ • Triggers   │  │ • Users      │              │
│  │ • Push       │  │ • Actions    │  │ • Reports    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. Database Schema Design

### 2.1 Core Entities (PostgreSQL)

```
ENTITY RELATIONSHIP DIAGRAM

┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│     USERS       │       │    CUSTOMERS    │       │  SUBSCRIPTIONS  │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id (PK)         │───┐   │ id (PK)         │───┐   │ id (PK)         │
│ email           │   │   │ user_id (FK)    │◄──┘   │ customer_id(FK) │◄──┐
│ password_hash   │   │   │ first_name      │       │ plan_id (FK)    │   │
│ email_verified  │   │   │ last_name       │       │ status          │   │
│ phone           │   │   │ ssn_encrypted   │       │ current_period  │   │
│ role            │   │   │ dob             │       │ stripe_sub_id   │   │
│ mfa_enabled     │   └──▶│ address         │       │ created_at      │   │
│ created_at      │       │ credit_goals    │       │ canceled_at     │   │
│ updated_at      │       │ status          │       └─────────────────┘   │
└─────────────────┘       │ created_at      │                             │
                          └─────────────────┘                             │
                                  │                                       │
                                  │                                       │
        ┌─────────────────────────┴─────────────────────────┐             │
        ▼                                                   ▼             │
┌─────────────────┐                               ┌─────────────────┐     │
│ CREDIT_REPORTS  │                               │    DISPUTES     │     │
├─────────────────┤                               ├─────────────────┤     │
│ id (PK)         │                               │ id (PK)         │     │
│ customer_id(FK) │◄──────────────────────────────│ customer_id(FK) │─────┘
│ bureau          │                               │ report_id (FK)  │
│ report_date     │                               │ tradeline_id    │
│ raw_data        │                               │ dispute_type    │
│ parsed_data     │                               │ status          │
│ score           │                               │ round           │
│ created_at      │                               │ letter_content  │
└─────────────────┘                               │ submitted_at    │
        │                                         │ resolved_at     │
        ▼                                         │ outcome         │
┌─────────────────┐                               └─────────────────┘
│   TRADELINES    │
├─────────────────┤
│ id (PK)         │
│ report_id (FK)  │
│ creditor_name   │
│ account_number  │
│ account_type    │
│ balance         │
│ credit_limit    │
│ payment_status  │
│ date_opened     │
│ remarks         │
│ is_negative     │
└─────────────────┘
```

### 2.2 Document Store (MongoDB)

```javascript
// Credit Report Document Schema
{
  _id: ObjectId,
  customerId: UUID,
  bureau: "experian" | "equifax" | "transunion",
  fetchedAt: ISODate,
  
  rawReport: {
    format: "metro2" | "pdf" | "api",
    data: Binary  // Encrypted raw data
  },
  
  parsedReport: {
    personalInfo: {
      names: [String],
      addresses: [{
        street: String,
        city: String,
        state: String,
        zip: String,
        reportedDate: ISODate
      }],
      employers: [String],
      ssn: String  // Last 4 only
    },
    
    creditScore: {
      score: Number,
      scoreType: "fico8" | "vantage3",
      factors: [String]
    },
    
    tradelines: [{
      id: String,
      creditorName: String,
      accountType: String,
      accountStatus: String,
      balance: Number,
      creditLimit: Number,
      paymentHistory: [{
        month: ISODate,
        status: String
      }],
      remarks: [String],
      isNegative: Boolean,
      disputeEligibility: {
        eligible: Boolean,
        reasons: [String],
        confidence: Number
      }
    }],
    
    publicRecords: [{
      type: String,
      filedDate: ISODate,
      court: String,
      amount: Number,
      status: String
    }],
    
    inquiries: [{
      creditor: String,
      date: ISODate,
      type: "hard" | "soft"
    }]
  },
  
  aiAnalysis: {
    analyzedAt: ISODate,
    overallScore: Number,
    negativeItems: [{
      tradelineId: String,
      issueType: String,
      severity: "low" | "medium" | "high",
      disputeStrategy: String,
      successProbability: Number
    }],
    recommendations: [String],
    projectedScore: {
      if_all_removed: Number,
      realistic_6mo: Number,
      realistic_12mo: Number
    }
  }
}
```

---

## 3. API Design

### 3.1 RESTful API Structure

```
API ENDPOINT STRUCTURE
══════════════════════

BASE URL: https://api.horizoncreditrepair.com/v1

AUTHENTICATION
├── POST   /auth/register
├── POST   /auth/login
├── POST   /auth/refresh
├── POST   /auth/logout
├── POST   /auth/forgot-password
├── POST   /auth/reset-password
├── POST   /auth/verify-email
├── POST   /auth/mfa/enable
├── POST   /auth/mfa/verify
└── DELETE /auth/mfa/disable

CUSTOMERS
├── GET    /customers/me
├── PATCH  /customers/me
├── DELETE /customers/me
├── GET    /customers/me/settings
├── PATCH  /customers/me/settings
├── POST   /customers/me/documents
├── GET    /customers/me/documents
├── DELETE /customers/me/documents/:id
└── POST   /customers/me/export

CREDIT REPORTS
├── GET    /credit-reports
├── GET    /credit-reports/:id
├── POST   /credit-reports/fetch
├── GET    /credit-reports/:id/tradelines
├── GET    /credit-reports/:id/analysis
├── GET    /credit-reports/score-history
└── GET    /credit-reports/compare

DISPUTES
├── GET    /disputes
├── GET    /disputes/:id
├── POST   /disputes
├── PATCH  /disputes/:id
├── DELETE /disputes/:id
├── GET    /disputes/:id/letters
├── POST   /disputes/:id/letters/generate
├── POST   /disputes/:id/submit
├── GET    /disputes/:id/timeline
└── GET    /disputes/statistics

SUBSCRIPTIONS
├── GET    /subscriptions/plans
├── GET    /subscriptions/current
├── POST   /subscriptions
├── PATCH  /subscriptions
├── POST   /subscriptions/cancel
├── POST   /subscriptions/resume
├── GET    /subscriptions/invoices
└── GET    /subscriptions/invoices/:id

NOTIFICATIONS
├── GET    /notifications
├── PATCH  /notifications/:id/read
├── POST   /notifications/mark-all-read
├── GET    /notifications/preferences
└── PATCH  /notifications/preferences

CHAT
├── GET    /chat/conversations
├── GET    /chat/conversations/:id
├── POST   /chat/conversations
├── POST   /chat/conversations/:id/messages
└── POST   /chat/conversations/:id/handoff
```

### 3.2 API Response Format

```typescript
// Success Response
interface ApiResponse<T> {
  success: true;
  data: T;
  meta?: {
    page?: number;
    limit?: number;
    total?: number;
    totalPages?: number;
  };
}

// Error Response
interface ApiError {
  success: false;
  error: {
    code: string;
    message: string;
    details?: Record<string, string[]>;
    requestId: string;
  };
}

// Example Success
{
  "success": true,
  "data": {
    "id": "cust_123",
    "firstName": "John",
    "lastName": "Doe",
    "creditScore": 645
  }
}

// Example Error
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid input data",
    "details": {
      "email": ["Email is already registered"]
    },
    "requestId": "req_abc123"
  }
}
```

---

## 4. User Journey Flows

### 4.1 Customer Onboarding Flow

```
CUSTOMER ONBOARDING SEQUENCE

┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  Start  │────▶│Register │────▶│ Verify  │────▶│ Payment │
│         │     │ Account │     │ Email   │     │  Setup  │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
                                                     │
     ┌───────────────────────────────────────────────┘
     ▼
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│Identity │────▶│  Fetch  │────▶│   AI    │────▶│  View   │
│ Verify  │     │ Reports │     │Analysis │     │  Plan   │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
                                                     │
                                                     ▼
                                               ┌─────────┐
                                               │ Active  │
                                               │ Client  │
                                               └─────────┘

DETAILED STEPS:

1. REGISTER ACCOUNT
   • Input: email, password, name, phone
   • Output: user_id, verification_token
   • Actions: Create user, send verification email

2. VERIFY EMAIL
   • Input: verification_token
   • Output: session_token
   • Actions: Mark verified, create session

3. PAYMENT SETUP
   • Input: plan_id, payment_method
   • Output: subscription_id
   • Actions: Create Stripe customer, setup subscription

4. IDENTITY VERIFICATION
   • Input: SSN, DOB, address
   • Output: verification_status
   • Actions: IDology verification, create customer profile

5. FETCH REPORTS
   • Input: customer_id, bureau_auth
   • Output: report_ids[]
   • Actions: Pull 3 bureau reports, parse, store

6. AI ANALYSIS
   • Input: report_ids[]
   • Output: analysis_results
   • Actions: Identify issues, score likelihood, generate plan

7. VIEW PLAN
   • Input: analysis_results
   • Output: action_plan
   • Actions: Present recommendations, set priorities
```

### 4.2 Dispute Processing Flow

```
DISPUTE LIFECYCLE STATE MACHINE

                    ┌─────────────────────────────────────────────┐
                    │                                             │
                    ▼                                             │
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌────────┴──┐
│ DRAFT   │──▶│GENERATED│──▶│SUBMITTED│──▶│ PENDING │──▶│  RESOLVED │
└─────────┘   └─────────┘   └─────────┘   └─────────┘   └───────────┘
     │             │             │             │              │
     │             │             │             │              ▼
     ▼             ▼             ▼             ▼         ┌─────────┐
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   │• DELETED│
│CANCELLED│   │ REVISED │   │ FAILED  │   │ESCALATED│   │• VERIFIED│
└─────────┘   └─────────┘   └─────────┘   └─────────┘   │• UPDATED │
                                               │        │• REMAINS │
                                               ▼        └─────────┘
                                         ┌─────────┐
                                         │  CFPB   │
                                         │COMPLAINT│
                                         └─────────┘

STATE TRANSITIONS:

DRAFT → GENERATED
  Trigger: User approves items, system generates letter
  Actions: Create letter, validate compliance

GENERATED → SUBMITTED
  Trigger: User confirms, system submits to bureau
  Actions: Send letter, record tracking, start timer

SUBMITTED → PENDING
  Trigger: Bureau acknowledges receipt
  Actions: Update status, notify user

PENDING → RESOLVED
  Trigger: Bureau responds (30-45 days)
  Actions: Parse response, update tradeline, notify

PENDING → ESCALATED
  Trigger: No response in 45 days / partial resolution
  Actions: Generate escalation letter, flag for specialist

ESCALATED → CFPB COMPLAINT
  Trigger: Continued non-response
  Actions: Prepare CFPB submission, assign to specialist
```

---

## 5. Data Flow Diagrams

### 5.1 Credit Report Processing Pipeline

```
CREDIT REPORT PROCESSING

[Customer Request]
       │
       ▼
┌─────────────────┐
│  API Gateway    │
│  (Validation)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  Report Service │────▶│   Bureau API    │
│  (Orchestration)│     │   (Experian)    │
└────────┬────────┘     └────────┬────────┘
         │                       │
         │◄──────────────────────┘
         │
         ▼
┌─────────────────┐
│   Job Queue     │
│   (BullMQ)      │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────┐
│Parser │ │ OCR   │
│Worker │ │Worker │
└───┬───┘ └───┬───┘
    │         │
    └────┬────┘
         │
         ▼
┌─────────────────┐
│  AI Analyzer    │
│  (Python)       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Data Store     │
│  (MongoDB)      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Event Bus      │
│  (Kafka)        │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────┐
│Notify │ │Update │
│Service│ │Search │
└───────┘ └───────┘
```

### 5.2 Real-Time Score Monitoring

```
SCORE MONITORING PIPELINE

     ┌─────────────────────────────────────────────────┐
     │            SCHEDULED JOB (Daily 2AM)            │
     └─────────────────────────────────────────────────┘
                            │
                            ▼
     ┌─────────────────────────────────────────────────┐
     │         FETCH ACTIVE CUSTOMER LIST              │
     │         (Batch: 1000 customers/run)             │
     └─────────────────────────────────────────────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │ Experian │  │ Equifax  │  │TransUnion│
        │   API    │  │   API    │  │   API    │
        └────┬─────┘  └────┬─────┘  └────┬─────┘
              │             │             │
              └─────────────┼─────────────┘
                            │
                            ▼
     ┌─────────────────────────────────────────────────┐
     │              COMPARE WITH PREVIOUS              │
     │         Score Δ > 5 points? New items?          │
     └─────────────────────────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              │                           │
              ▼                           ▼
     ┌─────────────────┐       ┌─────────────────┐
     │   SIGNIFICANT   │       │  NO CHANGE      │
     │     CHANGE      │       │  (Log only)     │
     └────────┬────────┘       └─────────────────┘
              │
              ▼
     ┌─────────────────────────────────────────────────┐
     │              TRIGGER NOTIFICATIONS              │
     │         Email, Push, SMS (per preference)       │
     └─────────────────────────────────────────────────┘
```

---

## 6. Security Architecture

### 6.1 Authentication Flow

```
JWT AUTHENTICATION FLOW

┌────────┐                     ┌────────┐                     ┌────────┐
│ Client │                     │  API   │                     │  Auth  │
│        │                     │Gateway │                     │Service │
└───┬────┘                     └───┬────┘                     └───┬────┘
    │                              │                              │
    │  POST /auth/login            │                              │
    │  {email, password}           │                              │
    │─────────────────────────────▶│                              │
    │                              │                              │
    │                              │  Validate credentials        │
    │                              │─────────────────────────────▶│
    │                              │                              │
    │                              │  {user, tokens}              │
    │                              │◀─────────────────────────────│
    │                              │                              │
    │  {accessToken, refreshToken} │                              │
    │◀─────────────────────────────│                              │
    │                              │                              │
    │  GET /api/resource           │                              │
    │  Authorization: Bearer xxx   │                              │
    │─────────────────────────────▶│                              │
    │                              │                              │
    │                              │  Verify JWT                  │
    │                              │────────┐                     │
    │                              │        │                     │
    │                              │◀───────┘                     │
    │                              │                              │
    │  {resource data}             │                              │
    │◀─────────────────────────────│                              │
    │                              │                              │

TOKEN SPECIFICATIONS:
─────────────────────
Access Token:
  • Algorithm: RS256
  • Expiry: 15 minutes
  • Claims: user_id, email, role, permissions

Refresh Token:
  • Algorithm: RS256
  • Expiry: 7 days
  • Rotation: On each use
  • Storage: HttpOnly cookie + Redis
```

### 6.2 Data Encryption Layers

```
ENCRYPTION ARCHITECTURE

┌─────────────────────────────────────────────────────────────────────┐
│                        ENCRYPTION LAYERS                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  LAYER 1: TRANSPORT (TLS 1.3)                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  All data encrypted in transit between:                      │   │
│  │  • Client ↔ API Gateway                                      │   │
│  │  • API Gateway ↔ Services                                    │   │
│  │  • Services ↔ Databases                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  LAYER 2: FIELD-LEVEL ENCRYPTION (AES-256-GCM)                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Sensitive fields encrypted before storage:                  │   │
│  │  • SSN (full)                                                │   │
│  │  • Date of Birth                                             │   │
│  │  • Full Address                                              │   │
│  │  • Bank Account Numbers                                      │   │
│  │  • Credit Card Numbers (if stored)                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  LAYER 3: DATABASE ENCRYPTION (AWS RDS)                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  • Encryption at rest using AWS KMS                          │   │
│  │  • Automated key rotation                                    │   │
│  │  • Encrypted backups                                         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  LAYER 4: FILE ENCRYPTION (S3)                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  • Server-side encryption (SSE-S3 or SSE-KMS)                │   │
│  │  • Client-side encryption for highly sensitive docs          │   │
│  │  • Signed URLs with short expiry                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 7. Integration Patterns

### 7.1 External Service Integration

```
INTEGRATION PATTERN: CIRCUIT BREAKER + RETRY

┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│  ┌─────────┐     ┌──────────────┐     ┌────────────┐               │
│  │ Service │────▶│Circuit Breaker│────▶│  External  │               │
│  │  Call   │     │              │     │    API     │               │
│  └─────────┘     └──────────────┘     └────────────┘               │
│                         │                                          │
│                         │                                          │
│       ┌─────────────────┼─────────────────┐                        │
│       │                 │                 │                        │
│       ▼                 ▼                 ▼                        │
│  ┌─────────┐      ┌─────────┐      ┌─────────┐                     │
│  │ CLOSED  │      │  OPEN   │      │HALF-OPEN│                     │
│  │(Normal) │      │ (Fail)  │      │ (Test)  │                     │
│  └─────────┘      └─────────┘      └─────────┘                     │
│                                                                     │
│  STATE TRANSITIONS:                                                 │
│  • CLOSED → OPEN: 5 failures in 60 seconds                         │
│  • OPEN → HALF-OPEN: After 30 second timeout                       │
│  • HALF-OPEN → CLOSED: Successful request                          │
│  • HALF-OPEN → OPEN: Failed request                                │
│                                                                     │
│  RETRY POLICY:                                                      │
│  • Max retries: 3                                                  │
│  • Backoff: Exponential (1s, 2s, 4s)                               │
│  • Jitter: ±500ms                                                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.2 Event-Driven Architecture

```
EVENT FLOW ARCHITECTURE

┌─────────────────────────────────────────────────────────────────────┐
│                         KAFKA EVENT BUS                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  TOPICS:                                                            │
│  ├── customer.created                                              │
│  ├── customer.updated                                              │
│  ├── credit-report.fetched                                         │
│  ├── credit-report.analyzed                                        │
│  ├── dispute.created                                               │
│  ├── dispute.submitted                                             │
│  ├── dispute.resolved                                              │
│  ├── subscription.created                                          │
│  ├── subscription.canceled                                         │
│  ├── payment.succeeded                                             │
│  └── payment.failed                                                │
│                                                                     │
│  EXAMPLE EVENT:                                                     │
│  {                                                                  │
│    "id": "evt_123",                                                │
│    "type": "dispute.resolved",                                     │
│    "timestamp": "2024-12-01T10:30:00Z",                            │
│    "data": {                                                       │
│      "disputeId": "disp_456",                                      │
│      "customerId": "cust_789",                                     │
│      "outcome": "deleted",                                         │
│      "bureau": "experian"                                          │
│    },                                                              │
│    "metadata": {                                                   │
│      "version": "1.0",                                             │
│      "source": "dispute-service"                                   │
│    }                                                               │
│  }                                                                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 8. Scalability Design

### 8.1 Horizontal Scaling Strategy

```
SCALING ARCHITECTURE

┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                        LOAD BALANCER (ALB)                          │
│                              │                                      │
│        ┌─────────────────────┼─────────────────────┐               │
│        ▼                     ▼                     ▼               │
│   ┌─────────┐           ┌─────────┐           ┌─────────┐          │
│   │ API Pod │           │ API Pod │           │ API Pod │          │
│   │   #1    │           │   #2    │           │   #3    │          │
│   └─────────┘           └─────────┘           └─────────┘          │
│        │                     │                     │               │
│        └─────────────────────┼─────────────────────┘               │
│                              ▼                                      │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                    REDIS CLUSTER                            │  │
│   │            (Sessions, Cache, Rate Limiting)                 │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                              │                                      │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                 POSTGRESQL (RDS Multi-AZ)                   │  │
│   │        Primary (Write) ───────▶ Replica (Read)              │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
│   AUTO-SCALING RULES:                                               │
│   • Scale up: CPU > 70% for 5 minutes                              │
│   • Scale down: CPU < 30% for 10 minutes                           │
│   • Min pods: 3                                                    │
│   • Max pods: 20                                                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

*Document Version: 1.0*
*Last Updated: December 2024*

